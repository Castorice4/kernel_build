# A simple action usage
name: Build Kernel
on:
  workflow_dispatch:

jobs:
  Build-Kernel:
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Build Kernel
        uses: Castorice4/kernel_build_action@main
        with:
          kernel-url: ghttps://github.com/Castorice4/kernel-crux
          kernel-dir: msm-4.14
          kernel-branch: SakuraPetals
          config: crux_defconfig
          arch: arm64
          aosp-gcc: true
          aosp-clang: true
          android-version: 12
          aosp-clang-version: r383902
          disable-lto: true
          anykernel3: true
